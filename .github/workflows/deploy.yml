name: Deploy to Vercel

on:
  push:
    branches: [main]

env:
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - run: bun install

      - name: Run database migrations
        run: bun run db:migrate
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - run: bun run vercel-build

      - run: bunx vercel pull --yes --environment production --token=$VERCEL_TOKEN

      # NODEJS_HELPERS=0 sets shouldAddHelpers:false in the function config, so
      # Vercel doesn't drain the request body before Hono reads it (POST routes).
      - run: bunx vercel build --prod --token=$VERCEL_TOKEN
        env:
          NODEJS_HELPERS: '0'

      - run: bunx vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN
